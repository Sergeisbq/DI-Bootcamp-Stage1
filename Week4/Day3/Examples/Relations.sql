-- one - to - one
-- israeli citizen <-> israeli passport

-- user <-> profile 




-- create table script (
-- movie_id int primary key,
-- title varchar(50),
-- author varchar(50),
-- story text,
-- constraint df_movie_id foreign key (movie_id) references movies(id)
-- );


-- insert into script (movie_id, title, author, story)
-- values
-- ((select id from movies where title = 'Pulp fiction'), 'Pulp fiction', 'Quentin Tarantino', 'Multiple stories about')


-- 

